version: 2
updates:
  # ZENITH currently has no npm, Python, Rust, or other package-manager
  # manifest in the repository. GitHub Actions are the dependency surface
  # that requires automated version/security update coverage today.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
